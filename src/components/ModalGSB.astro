<dialog id="my_modal_3" class="modal">
    <div class="modal-box max-w-[400px]">
      <form method="dialog">
        <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
      </form>
      <h3 class="text-lg font-bold mb-3">FREEBET BURE SIKU YA 3 🔥🔥🔥</h3>
      <hr>
      <p class="my-4">
        Beti mechi za ulaya siku 3 mfululizo tukurudishie nusu ya dau lako BURE! Siku ya tatu 🔥
      </p>
      <div class="mb-6">
        <img src="/ads/gsb-free-3.webp" alt="GSB - Tanzania" onclick="OpenAff('gsb')" />
      </div>
      <hr>
      <div class="flex justify-end mt-5">
        <button class="btn ms-4 p-4 bg-secondary text-base-200 rounded hover:bg-primary active:bg-primary" onclick="OpenAff('gsb')">JISAJILI SASA</button>
      </div>
    </div>
  </dialog>

<script is:inline>
    const modal = document.getElementById("my_modal_3");

    // Function to show modal
    function showModal() {
      if (!modal.isConnected) {
        document.body.appendChild(modal)
      }
        modal.showModal();
    }

    // Initial delay of 10 seconds
    setTimeout(showModal, 15000);

    // Set up recurring show every 15 seconds after modal is closed
    modal.addEventListener("close", () => {
        setTimeout(showModal, 60000);
    });
</script>
