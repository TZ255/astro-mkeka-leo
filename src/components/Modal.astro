<dialog id="my_modal_2" class="modal">
  <div class="modal-box max-w-sm bg-orange-50 p-0 rounded-lg shadow-lg">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2 text-orange-800 text-lg bg-orange-200 z-10">✕</button>
    </form>
    
    <!-- Header with gradient background -->
    <div class="bg-gradient-to-r from-orange-700 to-orange-900 p-4 text-white">
      <div class="text-xl font-bold flex items-center">
        <span class="mr-2">1000% MULTIBONUS</span>
        <span class="flex">🔥🔥🔥</span>
      </div>
    </div>
    
    <!-- Content area -->
    <div class="p-4">
      <p class="my-4 text-gray-700">
        Pata nafasi ya kushinda zaidi kwa kuongeza ushindi wako hadi 1000% na multibonus
      </p>
      
      <div class="relative mb-6 rounded-lg shadow hover:shadow-xl transition-all duration-300" onclick="OpenAff('888bet')">
        <img src="/ads/888bet-multibonus.webp" alt="888Bet Tanzania - Multibonus 1000%" class="w-full" />
      </div>
    </div>
    
    <!-- Footer with CTA button -->
    <div class="bg-orange-100 p-4 flex justify-between items-center">
      <div class="text-orange-800">
        <div class="font-bold">Tayari kushinda?</div>
        <div class="text-xs">Jisajili na mabingwa</div>
      </div>
      <button class="btn bg-orange-800 hover:bg-orange-900 text-white px-6 py-2 rounded-full font-bold" onclick="OpenAff('888bet')">JISAJILI SASA</button>
    </div>
  </div>
</dialog>

<script is:inline>
    const modal = document.getElementById("my_modal_2");

    // Function to show modal
    function showModal() {
      if (!modal.isConnected) {
        document.body.appendChild(modal)
      }
        modal.showModal();
    }

    // Initial delay of 10 seconds
    setTimeout(showModal, 15000);

    // Set up recurring show every 15 seconds after modal is closed
    modal.addEventListener("close", () => {
        setTimeout(showModal, 30000);
    });
</script>
