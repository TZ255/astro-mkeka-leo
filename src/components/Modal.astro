<dialog id="my_modal_2" class="modal">
  <div class="modal-box max-w-sm bg-lime-50 p-0 rounded-lg shadow-lg">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2 text-lime-800 text-lg bg-lime-200 z-10">✕</button>
    </form>
    
    <!-- Header with gradient background -->
    <div class="bg-gradient-to-r from-lime-700 to-green-800 p-4 text-white">
      <div class="text-xl font-bold flex items-center">
        <span class="mr-2">100% BONUS</span>
        <span class="flex">🔥🔥🔥</span>
      </div>
    </div>
    
    <!-- Content area -->
    <div class="p-4">
      <p class="my-4 text-gray-700">
        Pata nafasi ya kushinda zaidi. Weka tukuweke dabo!
      </p>
      
      <div class="relative mb-6 rounded-lg shadow hover:shadow-xl transition-all duration-300" onclick="OpenAff('betwinner')">
        <img src="/ads/betwinner-short.webp" alt="betwinner Tanzania - 100% BONUS" class="w-full" />
      </div>
    </div>
    
    <!-- Footer with CTA button -->
    <div class="bg-yellow-100 p-4 flex justify-between items-center">
      <div class="text-lime-800">
        <div class="font-bold">Tayari kushinda?</div>
        <div class="text-xs">Jisajili na mabingwa</div>
      </div>
      <button class="btn bg-lime-800 hover:bg-lime-900 text-white px-6 py-2 rounded-full font-bold" onclick="OpenAff('betwinner')">JISAJILI SASA</button>
    </div>
  </div>
</dialog>


<script is:inline>
    const modal = document.getElementById("my_modal_2");

    // Function to show modal
    function showModal() {
      if (!modal.isConnected) {
        document.body.appendChild(modal)
      }
        modal.showModal();
    }

    // Initial delay of 10 seconds
    setTimeout(showModal, 15000);

    // Set up recurring show every 15 seconds after modal is closed
    modal.addEventListener("close", () => {
        setTimeout(showModal, 30000);
    });
</script>
