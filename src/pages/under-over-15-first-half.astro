---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug"
import SomaZaidi from '@components/SomaZaidi.astro';
import HTOverUnder from '@components/HTOverUnder.astro';

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);

const title = "Mikeka ya Under/Over 1.5 Halftime ya Uhakika"
const desc = "Utabiri wa leo wa Under/Over 1.5 Halftime – Pata bashiri za mechi za leo, kesho, jumamosi na jumapili kwa chaguo la magoli Under/Over 1.5 kipindi cha kwanza. Utabiri makini kwa ushindi wa haraka!"
---

<BaseLayout sideBarActiveItemID="ht15" title={title} description={desc}>
  <div class="pb-12 mt-5">
    <div class="mb-8">
      <h1 class="lg:text-4xl text-2xl font-bold mb-2">Mkeka wa Over/Under 1.5 HT</h1>
      <p>
        Shinda beti zako kila siku na mikeka yetu ya uhakika ya utabiri wa jumla ya magoli kwa kipindi cha kwanza (Juu/Chini ya 1.5). Hapa chini tumekuwekea utabiri wa mechi za leo na kesho kwa chaguo hili la Total Goals First Half: Under/Over 1.5 (kwa kingereza)
      </p>
    </div>
    <hr>
    <HTOverUnder />
  </div>
<hr>
<SomaZaidi />
</BaseLayout>